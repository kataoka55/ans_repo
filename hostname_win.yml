---
- name: ホスト名確認
  hosts: vps_win
  become: true
  tasks:
    - name: hostname
      win_shell: hostname
      register: result
      ignore_errors: yes

    - name: msg
      debug:
        msg: "{{ result }}"
